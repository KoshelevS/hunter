@{
    ViewData["Title"] = "Candidates";
}
<h1>Candidates are listed here</h1>
<div ng-cloak>
    <div ng-controller="candidateController">
        <h1>
            Candidates <button class="btn btn-info" ng-click="add()"><span class="glyphicon glyphicon-plus"></span> Add</button>
        </h1>
        <div ui-grid="gridOptions" external-scopes="clickHandler" class="grid"></div>
    </div>
</div>

